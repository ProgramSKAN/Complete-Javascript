//in SETS
//order is not guaranteed unlike array
//duplicates not allowed
//no index-based access

// const ids = new Set(['Hi', 'from', 'set!']);
//ids[1];//invalid
// ids.add(2);
// ids.add(2);//not added since 2 is already there
// if (ids.has('Hi')) {
//   ids.delete('Hi');
// }

// for (const entry of ids.entries()) {
//   console.log(entry[0]);
// }
